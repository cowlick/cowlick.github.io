import {Router} from "~/model/Router";

export const state = () =>
  ({
    routes: [
      {
        title: "ホーム",
        link: "/"
      }
    ]
  } as Router);

export const mutations = {};

export const actions = {};
